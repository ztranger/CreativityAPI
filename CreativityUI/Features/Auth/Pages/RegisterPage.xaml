<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:CreativityUI.Features.Auth.ViewModels"
             x:Class="CreativityUI.Features.Auth.Pages.RegisterPage"
             x:DataType="viewModels:RegisterViewModel"
             Title="Register">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="12">
            <Label Text="Register" FontSize="28" HorizontalOptions="Center" />

            <Entry Placeholder="Phone" Keyboard="Telephone" Text="{Binding Phone}" />
            <Entry Placeholder="DisplayName" Text="{Binding DisplayName}" />
            <Entry Placeholder="Username (optional)" Text="{Binding Username}" />
            <Entry Placeholder="Password" Text="{Binding Password}" IsPassword="True" />

            <Button Text="OK" Command="{Binding SubmitCommand}" />

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />

            <Label Text="{Binding ResultMessage}"
                   LineBreakMode="WordWrap"
                   TextColor="Gray" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
